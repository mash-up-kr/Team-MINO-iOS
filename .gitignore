### Xcode
build/
DerivedData/
xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

### Swift Package Manager
.build/
.swiftpm/
Package.resolved
# 외부 의존(Alamofire)의 잠금은 추적한다 — 없으면 사람·CI 마다 다른 패치 버전으로 빌드된다.
# Alamofire 는 Networking 을 거친 전이 의존이라 앱 프로젝트에는 Package.resolved 가 생기지 않는다.
!Packages/Networking/Package.resolved

### macOS
.DS_Store

### Claude (개인 설정/훅 — 공용 추적 안 함)
.claude/settings.local.json
.claude/hooks/xcode-autobuild.sh

### QA 실행 산출물 (매 실행마다 재생성 — 스크린샷·판정 리포트)
qa-artifacts/

### Mino-harness 배치 기록 (make sync 가 남기는 로컬 상태)
.claude/.mino-harness-manifest

### 로컬 시크릿 (API 키 등) — 커밋 금지
App/Config/Secrets.xcconfig
